# JWT
JWT_SECRET=demobank-dev-secret-2024

# KFP Certificate — Railway (base64-encoded file contents)
# Encode locally: base64 -w 0 client.crt
# KFP_CERT_CRT=<base64>
# KFP_CERT_KEY=<base64>
# KFP_CERT_PASSPHRASE=

# KFP Certificate — local dev (path to certs directory, default: ./certs)
# CERT_PATH=./certs

# Talys Transactional Anti-Fraud
# TALYS_API_TOKEN=
